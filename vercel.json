{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/generateSignedQr", "dest": "/generateSignedQr" },
    { "src": "/validateScan", "dest": "/validateScan" }
  ]
}
